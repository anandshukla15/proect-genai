# Core RAG stack
# Pinned to the 0.3 line: langchain 1.x removed langchain.chains / RetrievalQA.
langchain>=0.3.0,<1.0
langchain-community>=0.3.0,<0.4
langchain-huggingface>=0.1.0,<0.3
faiss-cpu>=1.9.0
pypdf>=4.0.0

# Local embeddings (free, runs on CPU)
sentence-transformers>=3.0.0

# Free-tier LLM providers (install at least one) - pinned for core 0.3 compat
langchain-groq>=0.2,<0.4
langchain-google-genai>=2.0,<3.0

# UI + config
streamlit>=1.36.0
python-dotenv>=1.0.0
